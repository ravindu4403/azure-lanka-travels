[build]
  command = "npm ci --legacy-peer-deps --include=dev && npx next build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20.18.1"
  NPM_VERSION = "10"
  NEXT_TELEMETRY_DISABLED = "1"
